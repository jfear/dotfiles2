- defaults:
    link:
      create: true
      relink: true

- link:
    ~/.bash:
    ~/.bashrc:
    ~/.bash_profile:
    ~/.gitconfig:
    ~/.condarc:
    ~/.tmux.conf:
    ~/.shell:
    ~/.zsh:
    ~/.zshrc:

- shell:
  - ./bin/install_cli_tools.sh
